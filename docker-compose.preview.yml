services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.preview
    ports:
      - "4173:4173"
    environment:
      - BLOB_READ_WRITE_TOKEN=${BLOB_READ_WRITE_TOKEN}
      - JWT_SECRET=${JWT_SECRET}
